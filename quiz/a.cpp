#include<iostream>
#include<algorithm>
using namespace std;
int main(){
    int a, sum = 0;
    cin >> a;
    int arr[2][a];
    for (int i = 0; i < 2; i++)
    {
        for (int j = 0; j < a; j++)
        {
            cin >> arr[i][j];
        }
        
    }
    int n = 1;
    for (int i = 0; i < 2; i++)
    {
        for (int j = 0; j < a; j++)
        {
            if (arr[i][j] == arr[])
            {
                /* code */
            }
            
        }
        
    }
    
    
}